namespace MassacreStackFinderCs.Types;

// Helper type for serializing a ring
public struct Ring
{
    // No ring properties are interesting to us right now, but we need this type to be able to serialize/deserialize ring arrays 
}